stages:
 - build
 - test
 - package
 - deploy

build:
  stage: build
  script:
    - ls -al

test:
  stage: test
  script:
    - ls -al

package:
  stage: package
  script:
    - ls -al

build image:
  stage: package
  script:
    - ls -al

deploy:
  stage: deploy
  script:
    - ls -al